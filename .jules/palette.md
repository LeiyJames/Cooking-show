## 2024-03-24 - Accordion Accessibility
**Learning:** Interactive headers/accordions must use `<button>` tags with `aria-expanded` and `aria-controls` to be accessible, matching the expandable region’s `id`.
**Action:** Always verify expanding/collapsing sections explicitly link their toggle button to the expanding content wrapper via ARIA.
